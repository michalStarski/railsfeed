= form_with model: @post, local: true, scope: 'post' do |f|
  = f.label :title
  = f.text_field :title
  = f.label :content
  = f.rich_text_area :content
  = f.hidden_field :sub_id, value: params[:sub] 
  = f.submit
